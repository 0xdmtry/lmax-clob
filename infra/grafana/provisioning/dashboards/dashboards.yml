apiVersion: 1

providers:
  - name: clob-dashboards
    orgId: 1
    folder: CLOB
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards